import { StubPage } from "../../../components/StubPage";

export function Sorteio() {
  return (
    <StubPage
      titulo="Sorteio"
      descricao="Animacao de sorteio das equipes (Fisher-Yates via Edge Function realizar-sorteio)"
    />
  );
}
